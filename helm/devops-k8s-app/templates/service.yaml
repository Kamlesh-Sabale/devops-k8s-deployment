apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-k8s-app.fullname" . }}
  labels:
    {{- include "devops-k8s-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-k8s-app.selectorLabels" . | nindent 4 }}
